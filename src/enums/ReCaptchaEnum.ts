export enum ReCaptchaDomain {
    "google" = "www.google.com",
    "recaptcha" = "www.recaptcha.net",
}

export enum ReCaptchaScore {
    "Score1" = 0.1,
    "Score3" = 0.3,
    "Score7" = 0.7,
    "Score9" = 0.9,
}

export enum ReCaptchaReportStatus {
    "success" = "success",
    "error" = "error",
}
